package com.example.db_task;

import com.example.db_task.Repositories.CarRepository;
import com.example.db_task.Repositories.CarRepositoryImpl;
import com.example.db_task.Repositories.PersonRepository;
import com.example.db_task.Repositories.PersonRepositoryImpl;
import com.example.db_task.TableClasses.Cars;

import java.util.List;

public class Repotest {

/*
    PersonRepository personRepository = new PersonRepositoryImpl();
    List<Cars> carList= personRepository.getCarsByPerson("Erwin Lefavre");
    CarRepository carRepository = new CarRepositoryImpl();
    carList.forEach(cars -> System.out.println(carRepository.getCarData(cars)));*/

}
